[project]
name = "custom_ui"
authors = [
    { name = "Chaitanya Sherkhane", email = "chetan@clicentrix.com" }
]
description = "Custom UI for ERPNext"
requires-python = ">=3.10"
dynamic = ["version"]

[tool.flit.module]
name = "custom_ui"

[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"
